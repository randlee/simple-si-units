# Catalog

This directory holds the `units-x` catalog contract and machine-readable
generation inputs.

- `schema/units-catalog.schema.json` defines the Phase A catalog structure.
- `examples/phase-a-sample-catalog.json` proves the schema can express the
  required naming and conversion edge cases before the full catalog lands.
