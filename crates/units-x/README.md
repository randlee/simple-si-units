# units-x

Primary Rust deliverable crate scaffold for the `units-x` project.

This crate owns the Rust-first library surface only. Python extension entry
points live in the sibling `crates/units-x-python` scaffold so the core crate
does not carry a language-specific boundary.
